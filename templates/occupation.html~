<!DOCTYPE HTML>
<html>
  <head>
    <title>{{title}}</title>
  </head>
  <body>
    <h1>{{head}}</h1>
    {% for i in links %}
    <a href = '{{i}}'>Home</a>
    {% endfor %}

    <table>
    {% for i in table %}
      <tr>
	<td> i </td>
	<td> jobDict[i] </td>
      </tr>
    {% endfor %}
    </table>
  </body>

  
</html>
